docker {
    enabled    = true
    runOptions = '--user $(id -u):$(id -g)'
}

params {
    oasis_min_percentile = 10
}

process {
    withName: 'RANK_OASIS' {
        ext.prefix = 'test_antibody'
    }
}
